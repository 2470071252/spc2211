# 应用名称
spring.application.name=spring-aop

# 应用服务 WEB 访问端口
server.port=8080

spring.datasource.url=jdbc:mysql://localhost:3306/spring_test?characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&rewriteBatchedStatements=true
spring.datasource.username=root
spring.datasource.password=root

logging.level.cn.tedu.spring = debug

# 设置Spring的代理对象创建方式：
# true 使用 CGLib Proxy，
# false 时候 JDK Proxy 和 CGLib 之间动态选择
# spring.aop.proxy-target-class=false
